IF(UNIX)
	SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -Wextra -std=c++11")
ENDIF()

#INCLUDE_DIRECTORIES()

ADD_EXECUTABLE(${PROJECT_NAME} postp.cpp parser.cpp)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} v8)

ADD_SUBDIRECTORY(rs274ngc)
