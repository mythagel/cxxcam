IF(UNIX)
	SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -Wextra -std=c++11")
ENDIF()

ADD_SUBDIRECTORY(rs274ngc)

INCLUDE_DIRECTORIES(rs274ngc)
ADD_EXECUTABLE(driver driver.cc)
TARGET_LINK_LIBRARIES(driver rs274ngc)
