
IF(UNIX)
	SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -Wall -Wextra")
ENDIF()

INCLUDE_DIRECTORIES(${CXXCAM_INCLUDE_DIR}/postp )
ADD_LIBRARY(postp STATIC gcode.cpp)
#TARGET_LINK_LIBRARIES(postp )
